#Hard 
![[Screenshot_21.png]]




Упорядочить разработку, понять какие проблемы решаются, какие + -, быстро понять код по ключевым словам Proxy, Factory

Разработка большой командой неэффективна без паттернов

синглтон глобальный объект который виден через ссылку

фактори новый метод инстанциирования

билдер (квери билдеры) позволяет делать пошаговый алгоритм операции инстанциирования, делать эту опирацию как обобщенный интерфейс с пошаговым определением результатам (обчно чейнинг)

прототайп создавать экземпляры через наследование (ЭТО НЕ ПРОТОТИПНОЕ НАСЛЕДОВАНИЕ, прототайп клонирует объекты, в рототипном наследовании мы просто клонируемся)

адаптер врапперы стыковать абстракции разные контракты

//протоколы доступа хттп сокет етк

дектораторы расширение в декларативном описании

фасад скрытие сложности модуля за простым интерфейсом, добавление возможной защиты для модуля

прокси перехват доситупа который он в себе инкапсулирует и добавляет поведение, такое как логипрование и прочее

-у прокси объекта (не = паттерн но похоже) есть уникальные фишки по вытаскиванию параметров объектов, которые мы просто так не можем получить 

чейн оф респонсиб - данные передаются в цепочке вызовов каж функция проверяет она ли работает с данными если нет то передает, если да то обрабатывает только она - дает ожидаемый результат

командер - запрос/действие в виде объекта (литерал объекта??/джейсон)

стэйт 

интерпрейтер - создание дсл языков домэйн специфик языков для работы с сущностями

обсервабл?
стрим
синхронный асинхронный


медиатор центральная хрень которая взаимодействует с множеством элементов берет и передает и проч

моменто / ум создает и восстанавливает состояния (паттерн сага) анду реду (похож на паттерн стэйт, но моменто хранит всю историю и можно роллбэк сделать)

обсервер ивент эмиттер ивент таргет

стратегия возможность делать взаимозаменяемое поведение и подставлять по ключу - строке символу

темплэйт метод позволяет задать общие шаги алгоритма а потом у насдедников заменять определнные шаги

виситор разделяет алгоритм который делает что-то над таргетом и таргеты, чтобы они были слабо зацеплины и можно было менять алгоритм или таргеты 

открытый конструктор

стримы вместо наследования передается функция трансформации трнсформ стрим

и много других






