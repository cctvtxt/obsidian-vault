# Руководство по формированию Obsidian Vault 

Проект представляет собой базу знаний Obsidian, документ GEMINI.md содержит правила написания и структурирования заметок. 

1. Ключевой принцип — точность и емкость изложения.
2. Отсутствие воды, 
3. Только обезличенные утверждения, описания фактов 

Используй короткие и точные предложения. Избегай длинных абзацев.
# Общие правила формирования заметок

1. `[[ссылка]]` ссылка: добавляется в начале файла, определяет ссылку на существующий файл внутри Obsidian Valut, который раскрывает родительское понятие по отношению к текущей заметке. Ссылка на файл может отсутствовать. Сразу за ссылкой в начале файла следует абзац с кратким определением темы и раскрытием и упоминанием стандартов и источников темы, 
2. 
3. , без специального заголовка. Другие ссылки, кроме ссылки в начале файла, в тексте не допускаются.
4. `сплошной текст` основное текстовое содержимое: всегда перед любым абзацем вставляй необходимо добавление пустой строки. 
5. ` ` или `   ` или `\t` отступы перед фрагментами текста, табуляция: полностью запрещены
6. `\n` перенос строки: 
   
7. `#` заголовок первого уровня: Используй заголовки (`#`) умеренно. Не создавай множество мелких секций
   
   
   Перед заголовком никогда не добавляй пустую строку, заголовок идет в новой строке после теста перед ним
8. `##` заголовок второго уровня: 
9. `###` заголовок третьего уровня: добавление запрещено;
10. `*курсив*` выделение курсивом:
11. `**жирный**` выделение жирным:
12. `~~зачеркнутый~~` зачёркивание: 
13. `<u>подчёркнутый</u>` подчёркивание:
14. `***жирный курсив***` или `___жирный курсив___` жирный + курсив:
15. `` `код` `` моноширинный текст (inline code):
16. ```` ```блок кода ``` ```` блок кода: всегда используется в явным указанием языка программирования
17. `-` или `*` или `+` ненумерованный список: добавление запрещено;
18. `1.` нумерованный список: 
19. `> ` цитата: добавление запрещено;
20. `[текст](ссылка)` ссылка: добавление запрещено;
21. `![альт](изображение.jpg)` изображение: добавление запрещено;
22. `[текст][якорь]` ссылка на якорь: 
23. `[якорь]: https://example.com` определение якоря:
24. ``| Заголовок1 | Заголовок2 |`` таблица: 
25. `---` или `***` горизонтальная линия: добавление запрещено;
26. `___` горизонтальная линия:
27. HTML-форматирование: разрешено только при ручном вводе. 

Для отдельных директорий предусмотрены свои уникальные правила:
# Уникальные правила для `./NPM Registry`

Каталог предназначен для создания заметок, относящихся к 

**Полное Отсутствие Заголовков**: В файлах, описывающих NPM-библиотеки (например, стратегии Passport), заголовки (`#`) не используются вовсе. Весь текст должен представлять собой единое повествование, разделенное на абзацы и блоки кода.

**Структура Повествования**: Для NPM-библиотек текст должен последовательно раскрывать следующие аспекты, изложенные в свободной форме: основное назначение и принцип работы библиотеки, пример интеграции в виде блока кода, и развернутый анализ применимости, целесообразности, сравнение с альтернативами и важные аспекты использования.
