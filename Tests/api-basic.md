## API — базовый уровень

🏆 Правильных ответов: 10 из 10

#### Q1. Какую задачу решают параметры пути в REST API запросах? #flashcards/API/basic

- [ ] `Передают изменяющиеся части URL для доступа к конкретным ресурсам`
?
1. `Передают изменяющиеся части URL для доступа к конкретным ресурсам`

#### Q2. Какую задачу решает использование ReDoc при работе с API? #flashcards/API/basic

- [ ] `Визуализирует и создает статическую документацию API на основе спецификации OpenAPI`
?
1. `Визуализирует и создает статическую документацию API на основе спецификации OpenAPI`

#### Q3. В чем заключается ключевое преимущество использования JWT для аутентификации в API по сравнению с традиционным подходом сессий в распределённых системах? #flashcards/API/basic

- [ ] `JWT можно безопасно передавать между доменами, что удобно при работе с микросервисами`
?
1. `JWT можно безопасно передавать между доменами, что удобно при работе с микросервисами`

#### Q4. Какое основное функциональное сходство имеют OAuth 2.0 и Basic Auth? #flashcards/API/basic

- [ ] `Оба метода позволяют управлять доступом к ресурсам API`
?
1. `Оба метода позволяют управлять доступом к ресурсам API`

#### Q5. Клиент успешно прошёл аутентификацию, но у него нет разрешения на выполнение операции с указанным ресурсом

Какой код ошибки вернёт сервер в этом случае? #flashcards/API/basic

- [ ] `403`
?
1. `403`

#### Q6. Какое ключевое свойство метода POST следует учитывать при создании нового ресурса через API? #flashcards/API/basic

- [ ] `POST не является идемпотентным методом, и повторный запрос может привести к созданию дубликатов`
?
1. `POST не является идемпотентным методом, и повторный запрос может привести к созданию дубликатов`

#### Q7. Вы работаете с REST API и хотите обновить существующий ресурс. Вместо PUT или PATCH вы решили использовать POST

В чём основная проблема такого подхода? #flashcards/API/basic

- [ ] `POST создаст новый ресурс вместо обновления существующего`
?
1. `POST создаст новый ресурс вместо обновления существующего`

#### Q8. Какое преимущество использования параметра `offset` при реализации пагинации? #flashcards/API/basic

- [ ] `Позволяет задавать смещение результатов относительно первой записи`
?
1. `Позволяет задавать смещение результатов относительно первой записи`

#### Q9. Какой из перечисленных методов обычно применяют для асинхронного обмена данных в реальном времени? #flashcards/API/basic

- [ ] `WebSocket`
?
1. `WebSocket`

#### Q10. Какую задачу решает интеграция Swagger в проект с REST API? #flashcards/API/basic

- [ ] `Облегчение разработки и тестирования API с предоставлением генерируемой документации и интерактивного интерфейса для тестирования запросов`
?
1. `Облегчение разработки и тестирования API с предоставлением генерируемой документации и интерактивного интерфейса для тестирования запросов`
