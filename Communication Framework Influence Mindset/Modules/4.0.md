








1. Что такое - NodeJS?

_No, Jessica, runtime JavaScript accuracy объединяет. JobJava. 8. И Multiple BV, которые реализуют возможность обработки. Различных операций в асинхронном режиме и также она добавляет различные кор-модули, которые необходимы для разработки backend-приложений. Ноду используют для. Разработки бэкенда, микросервисов, реализации BFF, SSR, когда-то активно использовали для облачных вычислений и плохо. Она подходит для. Только для. Реализации CPU-bound задач. Это в целом потому что. JavaScript. Не самый производительный язык и, кроме того, он. Чувствителен к блокировке основного потока, потому что. В основе ноды лежит Асинхронная модель. Асинхронная модель обработки.

2. Зачем используют ноду, сравнение с другими языками (java, php). Где ноду лучше не использовать и почему? Что такое I/O и CPU bound операции


Из чего состоит nodejs?


кор модули
1. полноценно реализуем библиотеки
2. реализуем адаптер примитивов биндингов
биндинги - реализация логики на низком уровне, на котором мы можем реализовать обработку вне **MAIN** треда и работать с **ОС**
3. возможность использовать потоки тред пула ноды
4. возможность работать с **демулььтиплесором событий** ОС
библиотека libuv 
5. реализация таймеров
6. тред пул ноды
7. системные интерфейсы для вызовов процессов различных ОС 
8. ивент луп - бесконечный цикл, в очереди которого регистрируются все асинхронно выполняемые операции в ноде
движок в8
9. call stack
10. heap
11. сборщик мусора
------------
1. лексер - разбивает строку на токены
2. парсер - формирует AST дерево
3. интерпретатор ignition - генерит **байт-код**, собирает статистику
4. JIT-компилятор turbo fan: выполняет спекулятивную оптимизацию, генерит **машинный код**, деоптимизацию




ивент луп

- работает в основном потоке
- крутится все время

next tick
1. process.nextTick
микротаски
2. промисы
макротаски
3. таймеры
4. pending - io калбеки
5. idle prepare - системная фаза
6. poll - фаза в которой нода ожидает появления новых событий, в ней нода стоит если евент луп пуст
7. ckeck - set imeddiate
8. close - для close событий





9. Компоненты в архитектуре Node.js. Где находится сборщик мусора?
10. Фазы event loop. Где исполняется некст тик, а где микротаски. Каждую фазу знать не нужно, но важно понимать зачем они нужны. Тут советую порешать задачки по event loop (есть в базе знаний на странице event loop)
11. Что за тред пул и сколько там стоит по умолчанию поток. От чего зависит указания количества тредов в тред пуле? (от ядер процессора)
12. Что будет, если в эвент лупе не останется задач?
13. Как можно масштабировать Node.js приложение, если все же приходится работать с CPU bound операциями
14. Разница между Worker Threads и Child Process
15. Какие существуют методы промисов?
16. Разница между Thread pool и Worker Threads?

---

1. Жизненный цикл запроса. Нужно ультра четко понимать в какой последовательности компонентов неста обрабатываются запросы
2. DI, IoC, Injection scopes
3. На чем работает Nest.js (движок web сервера — express и можно поменять на фастивфай). Тут простая тема, просто держи в голове, что можно менять движок неста на express или фастивфай
4. Какие ошибки отлавливает Exception Filter?
5. Что такое useClass, useValue, useFactory, useExisting?
6. Что такое middleware в Express.js? (Поможет понимать лучше все сущности в NestJs)
7. Что такое динамические модули?

------

### **История и экосистема (1–5)**

1. В каком году Ryan Dahl впервые представил Node.js на конференции JSConf EU?
     
2. Кто создал npm и в каком году это произошло?
    
3. Почему возник io.js-форк и чем он отличался от основного Node.js на тот момент?
    
4. Когда произошло слияние io.js и Node.js, и что при этом было создано?
    
5. Какие три основных JavaScript-runtime обсуждаются в конспекте, и кто является автором каждого?
    

---

### **Runtime: общие понятия (6–10)**

6. Чем runtime отличается от самого языка JavaScript?
    
7. Какую ключевую проблему серверной разработки (связанную с масштабированием соединений) решал Node.js в своей первой презентации?
    
8. Почему JavaScript был выбран для Node.js вместо создания нового языка?
    
9. Что такое C10k problem и какие четыре механизма обычно используют для её решения?
    
10. Почему Node.js не является универсальным решением для всех серверных задач, несмотря на неблокирующий I/O?
    

---

### **V8: парсинг и компиляция (11–18)**

11. Как называется внутреннее представление программы, которое V8 строит из исходного кода?
    
12. Какой компонент V8 отвечает за разбиение строки кода на токены?
    
13. Что такое Ignition и какую роль он выполняет в V8?
    
14. Почему байт-код выгоднее полностью оптимизированного машинного кода для редко вызываемых функций?
    
15. Как называется оптимизирующий JIT-компилятор в V8 и на чём основан его подход к оптимизации?
    
16. Что происходит, если в «горячую» функцию, оптимизированную TurboFan под конкретные типы, внезапно передаются данные другого типа?
    
17. Назовите хотя бы три практики, которые затрудняют оптимизацию кода в V8.
    
18. Какой принцип работы использует Call Stack в V8?
    

---

### **Память и Garbage Collector (19–23)**

19. Как V8 делит память (Heap) для нужд сборщика мусора?
    
20. Чем отличается Young Generation от Old Generation в модели памяти V8?
    
21. Какие два флага позволяют ограничить размер старого и молодого поколений в Heap?
    
22. Если метрика `rss` растёт, а `heapUsed` остаётся стабильной, на какой тип утечки стоит подумать в первую очередь?
    
23. Назовите хотя бы три основные причины утечек памяти в Node.js-приложениях.
    

---

### **Event Loop: фазы и очереди (24–33)**

24. Назовите все шесть фаз Event Loop в Node.js в строгом порядке.
    
25. В какой фазе выполняются колбэки `setTimeout` и `setInterval`?
    
26. Почему фазу `idle / prepare` нельзя использовать для пользовательского кода?
    
27. В какой фазе Event Loop выполняются колбэки `setImmediate`?
    
28. Почему `setImmediate` гарантированно срабатывает после I/O, а `setTimeout(fn, 0)` — нет?
    
29. В какой момент выполняются микротаски `process.nextTick` — до, после или внутри фаз Event Loop?
    
30. Что произойдёт, если внутри обработки микротаски создать новую микротаску — она выполнится в текущем цикле или в следующем?
    
31. Какой принцип работы у очередей задач в Event Loop (FIFO или LIFO)?
    
32. Почему длинный синхронный код в колбэке блокирует обработку Promise и `async/await`?
    
33. Как изменился порядок выполнения `setTimeout` и `setImmediate` в libuv 1.45.0 (Node.js > 20)?
    

---

### **libuv и системный уровень (34–38)**

34. Что такое libuv и какую основную проблему она решает для Node.js?
    
35. Сколько потоков в Thread Pool libuv по умолчанию и какой максимум можно задать?
    
36. Чем Thread Pool libuv принципиально отличается от `worker_threads`?
    
37. Какие системные механизмы используются в System Interface libuv на Linux, macOS и Windows?
    
38. Что такое демультиплексор событий и какую роль он играет в паттерне Reactor?
    

---

### **Модули, биндинги и npm (39–44)**

39. Чем отличаются встроенные (internal) биндинги от нативных аддонов?
    
40. Какое расширение имеют скомпилированные нативные аддоны и как они подключаются?
    
41. Какой подход к модулям используется по умолчанию в Node.js: CommonJS или ES Modules?
    
42. Чем синхронный `require` отличается от асинхронного `import` на уровне спецификации?
    
43. Какие инструменты экосистемы (кроме самого npm) упоминаются в конспекте как важные для разработчика?
    
44. Назовите хотя бы пять Core Modules из стандартной библиотеки Node.js.
    

---

### **Streams, Buffer, Backpressure (45–48)**

45. Какие четыре вида стримов существуют в Node.js?
    
46. Почему Buffer появился в Node.js, если в стандартном JavaScript его не было?
    
47. Что такое backpressure и какой сигнал в Writable-стриме говорит о необходимости приостановить запись?
    
48. Чем Buffer принципиально отличается от обычного JavaScript-массива?
    

---

### **Promise API (49–51)**

49. Чем `Promise.race` отличается от `Promise.any` по поведению при ошибках?
    
50. Какой метод дождётся всех промисов и не бросит ошибку, даже если часть из них упадёт?
    
51. Какой метод вернёт массив результатов только в том случае, если **все** промисы завершились успешно?
    

---

### **Многопоточность и масштабирование (52–55)**

52. Чем Worker Thread отличается от Child Process с точки зрения изоляции и последствий падения?
    
53. Какой механизм используется для передачи данных между главным потоком и Worker Thread?
    
54. Что такое `SharedArrayBuffer` и в чём его ключевая особенность?
    
55. Для какой задачи предназначен модуль `cluster` в Node.js?
    

---

### **Сценарии использования и компромиссы (56–60)**

56. Почему Node.js считается почти идеальным выбором для BFF (Backend for Frontend)?
    
57. В каких случаях для CPU-bound задач лучше сразу выбрать другой язык, а не использовать Worker Threads в Node.js?
    
58. Какие два основных риска использования Bun в production упоминаются в конспекте?
    
59. Какую ключевую слабость Deno отмечают по сравнению с Node.js?
    
60. Какие три уровня жизненного цикла версий Node.js существуют между статусом Current и End of Life?